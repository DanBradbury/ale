Before:
  call ale#assert#SetUpLinterTest('vim', 'vinter')

After:
  call ale#assert#TearDownLinterTest()

Execute(Executable should default to vinter):
  AssertLinter 'vinter', ale#Escape('vinter') . ' --format json --stdin %s'